# ===========================================================
# Requirements — Google Colab & Linux (Python 3.11, CUDA 12.x)
# ===========================================================
# Usage:
#   pip install -r requirements.txt          # fresh Linux machine
#   !pip install -r requirements.txt         # Google Colab cell
#
# Strategy:
#   - Pre-installed packages on Colab use ">=" lower bounds so
#     pip accepts Colab's existing versions without reinstalling.
#   - Packages not in Colab use "==" for reproducibility.
# ===========================================================

# --- Core Compute Stack ---
# Colab pre-installs GPU-enabled torch; ">=" accepts it as-is.
# On a fresh Linux box, pip will install a compatible build.
torch>=2.2.0
torchvision>=0.17.0

# --- Standard Data Science ---
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
joblib>=1.3.0
tqdm>=4.65.0

# --- Hugging Face Ecosystem ---
# transformers & huggingface_hub are pre-installed on Colab;
# ">=" keeps Colab's newer version without downgrading.
transformers>=4.38.0
huggingface_hub>=0.21.0     # directly imported by ViT_Cifar100.py
peft==0.9.0                 # LoraConfig / get_peft_model
accelerate>=0.27.0          # required by peft / transformers
timm==0.9.16                # vision backbones (ViT, ResNet)
sentencepiece==0.2.0        # tokenizer dependency
datasets>=2.18.0            # HuggingFace dataset loader
evaluate>=0.4.1             # metrics calculation

# --- Quantization ---
# bitsandbytes Linux wheels are published without a +cuXXX suffix.
bitsandbytes>=0.43.3

# --- Utilities ---
objgraph>=3.6.0             # memory-leak debugging
triton>=2.2.0               # JIT compiler; pre-installed on Colab/Linux

